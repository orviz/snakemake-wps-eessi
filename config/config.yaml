# ==================
# EESSI & EASYBUILD
# ==================

# Absolute path to the EESSI initialization script.
eessi_init_script: "/cvmfs/software.eessi.io/versions/2025.06/init/bash"

# Path to the EasyBuild recipe (EasyConfig) file for WPS.
wps_easyconfig_path: "config/WPS-4.6.0-custom.eb"

# -----------------------------------------------------------------------------
# GIT SOURCE CONTROL PARAMETERS (For Custom Modifications)
# -----------------------------------------------------------------------------
# URL of the GitHub repository containing WPS (official or your custom fork).
# Default official: "https://github.com/wrf-model/WPS"
wps_git_repo: "https://github.com/wrf-model/WPS"

# The specific version, tag, branch, or commit hash you want to compile.
# Examples: "v4.6.0", "my-physics-patch-branch", "a1b2c3d4"
wps_git_version: "v4.6.0-patch1"
